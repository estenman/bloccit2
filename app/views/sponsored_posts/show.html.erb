<h1><%= @sponsor.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @sponsor.body %></p>
    <p>$<%= @sponsor.price %></p>
  </div>
  <div class="col-md-4">
    <%= link_to "Edit", edit_topic_post_path(@sponsor.topic, @sponsor), class: 'btn btn-success' %>
  </div>
</div>
